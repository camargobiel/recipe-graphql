generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

// -----> tables <------

model recipes {
  id    Int    @id @default(autoincrement())
  title String @unique
  description  String
}
